import express from 'express'
import Generator from '../class/Generator.js'

const generate = express()

// falta colocar status code
generate.get('/', (req, res) => {
    try {
        
    } catch(e) {

    }
})

export default generate
